# Email Service Configuration (Resend)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=your_resend_api_key_here

# Email sender address (must be verified in Resend)
# For testing, you can use: onboarding@resend.dev
# For production, use your verified domain email
EMAIL_FROM=onboarding@resend.dev

# Application URL (used for password reset/onboarding links AND CORS origin validation)
# IMPORTANT: This is also used for CORS security - must match your frontend domain
# Development: http://localhost:3000 or http://localhost:3001
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# JWT Authentication Secrets (REQUIRED - minimum 32 characters)
# CRITICAL: These secrets protect ALL user sessions and authentication
# NEVER use these example values in production - generate your own!
#
# Generate your own secrets using this command (run it twice):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Example format (DO NOT USE THESE - GENERATE YOUR OWN):
# JWT_SECRET=5c99a92f097386860ead19c24896b6f3d0416b6666564002c49cb4c2cb7f0861
# JWT_REFRESH_SECRET=c3e5cea19c24cb3561a8d6fc9f2eb20d0a58897ab064e6e799bd54fdf6cdd7f8
#
# Replace these placeholder values:
JWT_SECRET=
JWT_REFRESH_SECRET=

# Data Encryption Key (REQUIRED - minimum 64 characters)
# CRITICAL: This key encrypts sensitive PII data like SSNs at rest
# NEVER use the example value in production - generate your own!
#
# Generate your encryption key using this command:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Example format (DO NOT USE THIS - GENERATE YOUR OWN):
# ENCRYPTION_KEY=1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p7q8r9s0t1u2v3w4x5y6z7a8b9c0d1e2f
#
# Replace this placeholder value:
ENCRYPTION_KEY=

# Optional: Node Environment
# NODE_ENV=development
# NODE_ENV=production

# Optional: Netlify Deployment
# NETLIFY=true
