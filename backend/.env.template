# ============================================================================
# Robert Ventures InvestorDesk - Backend Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git (it's in .gitignore)

# ============================================================================
# SUPABASE CONFIGURATION (REQUIRED)
# ============================================================================
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# ⚠️ IMPORTANT: Use "service_role" secret, NOT "anon" key!
# The service_role key bypasses Row Level Security (needed for backend)

# ============================================================================
# CORS CONFIGURATION (REQUIRED)
# ============================================================================
# Comma-separated list of allowed origins (no spaces!)
# Add your frontend URLs here
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# For production, add your deployed frontend URL:
# CORS_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app

# ============================================================================
# JWT SECRET (REQUIRED)
# ============================================================================
# Generate a secure random key (32+ characters)
# macOS/Linux: openssl rand -hex 32
# Or use: https://generate-secret.vercel.app/32
JWT_SECRET=your-secret-key-change-in-production

# ⚠️ IMPORTANT: Never use default value in production!

# ============================================================================
# SERVER CONFIGURATION (OPTIONAL)
# ============================================================================
PORT=8000
ENVIRONMENT=development

# For production deployment:
# ENVIRONMENT=production

# ============================================================================
# EMAIL CONFIGURATION (OPTIONAL - for email verification)
# ============================================================================
# If you want real email verification (not test mode):
# ENABLE_EMAIL_VERIFICATION=true
# SENDGRID_API_KEY=your-sendgrid-api-key
# FROM_EMAIL=noreply@yourdomain.com

# Test mode (default): Use code 000000 for all verifications
# ENABLE_EMAIL_VERIFICATION=false

# ============================================================================
# NOTES
# ============================================================================
# - Restart the backend after changing any values
# - Don't use quotes around values
# - Don't add spaces around = sign
# - Keep this file secure (never commit to git)
